# Frontend env. Copy to .env.local and fill in.
# Base URL of the FastAPI backend. In dev, Vite also proxies /api -> :8000.
VITE_API_BASE_URL=/api/v1
# Google Maps JavaScript API key (Maps JS + Places). Required for the map view.
VITE_GOOGLE_MAPS_API_KEY=
